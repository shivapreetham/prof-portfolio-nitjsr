// utils/uploadFile.js
// Supabase configuration removed - no file upload functionality needed

export const uploadFile = async (file, config = {}) => {
  return {
    success: false,
    error: 'File upload functionality has been disabled'
  };
};
